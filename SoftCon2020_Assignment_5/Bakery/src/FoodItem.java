import java.math.BigDecimal;

/**
 * abstract class which defines how a typ of food (product like cake or decorator like cream) has to be
 */
public abstract class FoodItem {
    public abstract BigDecimal price();
}
